package pl.wroc.pwr.student.acteditor.paarsing;

import org.junit.Test;

import pl.wroc.pwr.student.acteditor.model.Schema;
import pl.wroc.pwr.student.acteditor.parsing.XSDParser;

public class XSDParserTest {

	@Test
	public void testGetElement() {
		String elementName = "rozporzadzenie";
		XSDParser parser = new XSDParser(Schema.getSchemaContent());
		
		parser.getElement(elementName);
	}

}
